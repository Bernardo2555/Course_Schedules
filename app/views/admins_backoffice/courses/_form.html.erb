<%= form_with(model: [:admins_backoffice, @course], local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code_c %>
    <%= form.text_field :code_c %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :ideal_year %>
    <%= form.number_field :ideal_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
