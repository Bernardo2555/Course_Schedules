<p id="notice"><%= notice %></p>

<h1>Administradores</h1>

<table>
  <thead>
  <tr>
    <th><%= @admins.human_attribute_name(:email) %></th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @admins.each do |admin| %>
    <tr>
      <td><%= admin.email %></td>
      <td><%= link_to t('links.edit'), edit_admins_backoffice_admin_path(admin) %></td>
      <td><%= link_to t('links.delete'), admins_backoffice_admin_path(admin), method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to t('links.new'), new_admins_backoffice_admin_path %>